export interface permission {
   nom:string
    
  }
  export interface role_permision {
   nom:string
   
  }
  export interface UserData {
    id: string;
    name: string;
    progress: string;
    fruit: string;
  }
  export interface UserData {
    role: string;
    ajouter: string;
    modifier: string;
    afficher: string;
  }